{
  "private": true,
  "scripts": {
    "dev": "pnpm -F @mpa/web dev",
    "clean": "pnpm -F '@mpa/*' clean",
    "build": "pnpm -F '@mpa/*' run build",
    "synth": "pnpm run clean && pnpm run build && pnpm -F @mpa/adapter synth",
    "deploy": "pnpm run clean && pnpm run build && pnpm -F @mpa/adapter run deploy",
    "migrate": "pnpm -F @mpa/db run migrate",
    "db:nuke": "pnpm -F @mpa/db run nuke",
    "db:seed": "pnpm -F @mpa/db run seed",
    "test": "pnpm -F @mpa/db run test",
    "gen": "prisma generate",
    "lint": "prettier --check --plugin-search-dir=. . && eslint .",
    "format": "prettier --write --plugin-search-dir=. ."
  },
  "name": "mpa-toolkit",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@mpa/cli": "workspace:^1.0.0",
    "@typescript-eslint/eslint-plugin": "^5.33.0",
    "@typescript-eslint/parser": "^5.33.0",
    "eslint": "^7.32.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-define-config": "^1.6.0",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-svelte3": "^3.4.1",
    "prettier": "^2.7.1",
    "prettier-plugin-svelte": "^2.7.0",
    "prisma": "^4.2.1",
    "rimraf": "^3.0.2",
    "svelte": "^3.49.0",
    "tsup": "^5.12.9",
    "typescript": "^4.7.4"
  }
}
